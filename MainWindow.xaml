<Window x:Class="ConfigurationManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConfigurationManager"
        mc:Ignorable="d"
        Title="配置管理器" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Factory：" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbFactory" Grid.Column="1" Height="25"
                         SelectionChanged="CmbFactory_SelectionChanged">
                    <ComboBoxItem Content="A"/>
                    <ComboBoxItem Content="B"/>
                    <ComboBoxItem Content="ALL"/>
                </ComboBox>
            </Grid>
            <Button x:Name="btnConfirm" 
                    Content="配置" 
                    Width="120" 
                    Height="40"
                    Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
